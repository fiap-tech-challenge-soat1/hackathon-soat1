# Autenticando requisições

Para autenticar as requisições, inclua o cabeçalho (header) **`Authorization`** com o valor **`"Bearer {SEU_TOKEN_AQUI}"`**.

Todas os endpoints que requerem autenticação estão devidamente marcados com `requer autenticação` na documentação abaixo.


